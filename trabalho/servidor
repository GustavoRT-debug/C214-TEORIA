package com.inatel.web;

public class ServidorRemoto {
    public String getHorarioDeAtendimento() {
        // Implemente o código para fazer a chamada ao servidor remoto e retornar o JSON.
        return ""; // Substitua com o JSON real retornado pelo servidor.
    }
}
